# CrazyPoppingCatz

Construct 3 project files for the game.

## Open

Open `project.c3proj` in Construct 3.

## Notes

- Assets live in `images/`, `sounds/`, and `fonts/`.
- Gameplay logic is in `eventSheets/` and `scripts/`.
